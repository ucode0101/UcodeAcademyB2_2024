package day_18_nested_loop_arrays_intro;

public class LoopsPractice {
    public static void main(String[] args) {

        // write a program to replace if
        // first and last index are even,  swap them
        // if they are odd convert the string to upper case and reverse it

        String str = "selenium";


        int firstIndex = str.indexOf(str.charAt(0));
        int lastIndex = str.lastIndexOf(str.charAt(str.length()-1));
        // now use if condition to check







    }
}
